services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: snake-backend-prod
    environment:
      - PORT=8020
      - WEBRTC_TURN_IP=213.14.134.174
    expose:
      - "8020"
    ports:
      - "8020:8020"
    networks:
      - snake-prod-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - API_URL=https://snake.li1.nl
    container_name: snake-frontend-prod
    expose:
      - "80"
    ports:
      - "8040:80"
    environment:
      - WEBRTC_TURN_IP=213.14.134.174
    networks:
      - snake-prod-network
    restart: unless-stopped

networks:
  snake-prod-network:
    driver: bridge

